created with jdk-13.0.1 and Eclipse

STEPS TO RUN THE PROGRAM:
+ run sql developer 11g
+ go to scripts folder
+ run movie_database.sql
+ run movie_database_2.sql and make sure everything compiles
( you might need to compile each of them individually )
+ run FinalVersion.jar in cmd which is in the test folder